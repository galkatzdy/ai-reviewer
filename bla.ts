import { helper } from 'helper';

enum Colors {
  Red = 'RED',
  Blue = 'BLUE',
  Yellow = 'YELLOW',
}

enum Sizes {
  Small = 'SMALL',
  Medium = 'MEDIUM',
  Large = 'LARGE',
}

enum Variants {
  Default = 'DEFAULT',
  Primary = 'PRIMARY',
  Secondary = 'SECONDARY',
}

const test = () => {
  const result = 2 + 2;
  console.log({ result });
};
