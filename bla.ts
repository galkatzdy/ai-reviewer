enum Colors {
  Red = 'red',
  Blue = 'blue',
  Yellow = 'yellow',
}

enum Sizes {
  SMALL = 'small',
  MEDIUM = 'medium',
  LARGE = 'large',
}

enum VARIANTS {
  Default = 'default',
  Primary = 'primary',
  Secondary = 'secondary',
}

const test = () => {
  const result = 2 + 2;
  console.log({ result });
};
